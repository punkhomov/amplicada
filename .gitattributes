# Корпус настоящих пакетов хранится дословно.
#
# Текстовые файлы проекта всегда хранятся и открываются с LF, в том числе на Windows.
* text=auto eol=lf

# Для чужого тестового корпуса точные байты, включая CRLF, важны для проверки парсера.
packages/learning-parser/fixtures/** -text
