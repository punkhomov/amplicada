/**
 * URL страницы логина по паролю. Единый источник и для фронтенд-роута модуля,
 * и для дескриптора метода, который модуль публикует узлу через `auth-node`.
 */
export const PASSWORD_LOGIN_PATH = '/auth/password/login';
